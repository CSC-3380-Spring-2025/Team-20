export interface EventCardProps {
  title: string;
  description: string;
  totalInterested: string; 
}

export interface Event {
  title: string;
  description: string;
  totalInterested: string; 
}
